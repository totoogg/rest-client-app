export interface TranslationTypes {
  //   Используем схему componentName.field
  page: {
    header: string;
    footer: string;
  };
  navLink: {
    signIn: string;
    signUp: string;
    sighOut: string;
  };
}
